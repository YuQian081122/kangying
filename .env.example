# 管理員密碼（必填，用於 /upload 後台登入）
# 建議使用 bcrypt 雜湊值，例如：$2b$10$xxxxx
# 也支援明文密碼（不建議用於正式環境）
ADMIN_PASSWORD=請更換為強密碼

# 資料目錄（Docker 預設掛載到 /data，本地開發可改為 ./data）
DATA_DIR=/data

# Docker compose 中的 data volume 綁定路徑
# 請改為 NAS 上實際的共享資料夾路徑
DATA_PATH=./data
